name: Workflow

on:
  push
  
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Production deployment step
        run: echo "This is the improved ci2 deployment workflow  🚀🚀"
